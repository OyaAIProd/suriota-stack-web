---
name: suriota-pagespeed-project-2026-06-02
description: "PageSpeed/Lighthouse optimization project for suriota.com — baseline, targets, tooling, what's done, and blockers"
metadata: 
  node_type: memory
  type: project
  originSessionId: 377d3655-a5aa-44ed-afcd-3e213cc7c1d6
---

Goal (set 2026-06-02): raise PSI scores on ~10 priority EN pages to **A11y/BP/SEO ≥95, Performance ≥90 (green) mobile**, measured mobile+desktop via PageSpeed Insights API. Spec: `docs/superpowers/specs/2026-06-02-pagespeed-optimization-design.md`. Scope = homepage + 6 product pages + iiot/system/surge pillars.

**Tooling (works):** `audit/psi_audit.py` — reads `PSI_API_KEY` from `.env`, batches mobile+desktop via PSI API, saves raw JSON to `audit/psi/<date>/`, prints scorecard + opportunity matrix. Resumable (delete a page's JSON to re-measure). PSI_API_KEY stored in `.env` (gitignored). Local `npx lighthouse` available but scores noisier than PSI — use only for trend.

**Baseline 2026-06-02 (authoritative PSI):** SEO **100** ✅, Best-Practices **96** ✅ on ALL pages (targets already met). A11y **94–95**. Performance mobile 44–85 (modbus 44, surge 55, waste-water 62, home 65), desktop 66–91.

**Root causes (data):**
- Unused JavaScript = #1 opportunity, **4990ms across 20 page×strategy** (third-party: Tawk/GA4/AdSense/Meta/LinkedIn).
- LCP mobile 5–8s on several pages → heavy **PNG posters** (e.g. modbus `GTWY-SRT-VD-726x1024.png`, also the LCP element).
- CLS: modbus mobile **0.883** — breakdown: whole `<main>` shifts **0.722** (top-of-page; likely **font load** — no preload, no inline @font-face, fonts via external CSS; NOT the entry-title which is already head-hidden) + poster widget 0.088. iiot/system/surge 0.305.
- A11y `color-contrast` (all-or-nothing, 13 nodes): brand amber `#c8851f` used as TEXT on light bg (hero-eyebrow, pillar-num, a heading, whyus-num) — but same amber is on DARK bg elsewhere (must stay bright → no global darken); + label gray `#95a4ab` (2.56:1); + body gray `#7a7a7a` (4.29:1, theme-level, NOT in snippets). `heading-order` (an h4) + `label-content-name-mismatch` (sx-action-btn: visible "Order on Tokopedia" ≠ aria-label "Order SRT-MGATE-1210 on Tokopedia") also fail.

**DONE:** Fixed contained contrast in snippet **5656** (related-pillars): `.sx-related-pillars__label` `#95A4AB`→`#5C6B73`, `.sx-related-pillar__num` `#C8851F`→`#A66A12` (hover border amber untouched). Backup `audit/snippet_backup_2026-06-02/5656_orig.txt`. Verified live.

**KEY DISCOVERY:** Elementor **snippet** edits (elementor_snippet CPT, head/body_end injected CSS/JS) go **LIVE immediately via wp/v2 REST — NO Playwright/Elementor flush needed** (unlike `_elementor_data` builder edits, which DO need the flush). So CSS/JS-snippet-based fixes are fully autonomous via curl+App Password.

**BLOCKERS (need user, 2026-06-02):**
1. **Cloudflare API token EXPIRED 2026-06-01** (`.env` CF_API_TOKEN, status=expired). Blocks the chosen image path (CF Polish WebP) — the biggest LCP lever. Need a fresh CF token (Zone Settings: Edit) OR user enables Polish in dashboard (Polish = Pro plan only). Fallback = EWWW WebP (installed) but needs admin UI.
2. **Playwright + suriota-wp MCP disconnected mid-session** → can't flush Elementor builder cache (for `_elementor_data` image-dimension/CLS fixes), can't run EWWW bulk, can't edit theme/plugin files, can't visually verify. Reconnect via Claude Code restart.

**Snippet-free runbook (ready to execute):** `docs/superpowers/specs/2026-06-02-pagespeed-runbook.md` — per-problem native fixes (EWWW images, child-theme font preload+display, WP-Optimize delay-JS, per-context contrast), honoring user rules (NO code-snippet plugin, NO Cloudflare). Needs a child theme + admin tooling (Playwright/EWWW) to apply.

**Remaining work when unblocked:** images→WebP (LCP, needs CF token or EWWW), font preload+display (CLS 0.722), third-party JS defer (unused-JS 4990ms — risky to tracking, do carefully), per-context amber-contrast + #7a7a7a body (A11y→95, needs visual check; blanket override would break text on dark sections). Re-measure each via `psi_audit.py`.

---
## SESSION 2026-06-02 (eve) — partial run, then Playwright crash

**Tooling this session:** REST/curl (App Password) = WORKS (200). Playwright = worked initially (login OK) then **CRASHED during customize.php navigation** ~16:09 and did not re-register (needs Claude Code/MCP restart). suriota-wp MCP = DEAD 401 (as always). NOTE: admin-ajax (EWWW bulk, Elementor "Clear Files & Data" flush, Customizer save, WP-Optimize) needs **cookie session** — App Password/curl CANNOT do these. So browser is mandatory for activation.

**A11y root-cause map (from baseline PSI JSON, authoritative):** every measured page fails 3 audits → `color-contrast` + `heading-order` + (6 product pages also) `label-content-name-mismatch`. Pages already ≥95 except 6 product pages at 94 (the extra `label-content-name-mismatch` is what drops them).
- color-contrast specifics: `#7A7A7A`(p/li, 4.29) = **Elementor Global Text** (`.elementor-kit-5 --e-global-color-text`); `#C8851F`(hero-eyebrow/related-num/section-H2, 2.79–3.07) = **Global Accent** (used on DARK too → must NOT change globally); related-label `#95A4AB`→already #5C6B73 live (5656); homepage portfolio `.sx-pl-count`#8b9ca3 + `.sx-pl-yb.y-2025`#a56d11. Target colors (≥4.5:1): amber-on-light=**#946012** (white 5.33/cream 4.84); gray label/count=**#5C6B73**; body text=**#646464**. NOTE 5656's #A66A12 is only ~4.1 → STILL FAILS, must go darker.
- heading-order: footer template **1079** had 3× `header_size:h4` (Our Services/Products/Connect with Us) → content-h2→footer-h4 skip.
- label-mismatch: product `.sx-action-btn` in `_elementor_data` widget — EN aria "Order {SKU} on Tokopedia" not ⊇ visible "Order on Tokopedia".

**DONE & LIVE-via-REST this session (staged in `_elementor_data`, NEED Elementor flush to render):**
- aria-label fixed on 6 EN product pages (934/929/1740/1741/1742/1765): `Order on Tokopedia — {SKU}` + `Download Datasheet — {SKU}`. Backups `audit/elementor_backup_2026-06-02/{id}_{name}_orig.json`. Verified persisted via REST.
- footer 1079: 3× h4→h2. Backup `audit/elementor_backup_2026-06-02/footer_1079_orig.json`.

**DONE & LIVE (settings, no flush needed):**
- EWWW: **WebP Conversion ON + Picture WebP Rewriting ON** (JS-free, best for LCP; graceful fallback to original where no .webp). Bulk (Force+WebP-only, foreground) reached **~829/2642** before Playwright crash → only 2026/* folder images got .webp; **older folders (2023 logo, 2025/01 product icons, posters) NOT yet converted**. Must RE-RUN bulk (Force+WebP-only, **Background mode** for crash-resilience) to finish. cwebp works (02-logo.jpg.webp=200). Note posters uploaded natively as .webp (energy-meter-iot-poster.webp) need no conversion.

**NEXT (browser required — do after MCP reconnect):**
1. EWWW bulk Force+WebP-only **Background mode** → finish all 2642. Verify webp-on-disk for logo + product icons.
2. **Elementor flush** ("Clear Files & Data") + WP-Optimize purge → activates aria + footer fixes AND Picture-webp HTML rewrite. Re-check modbus served HTML: new aria present, footer h2, `<picture>` count >0.
3. Contrast via **Customizer→Additional CSS** (core, non-snippet; child theme NOT creatable autonomously — no filesystem channel since suriota-wp MCP dead): paste block →
   `body.elementor-kit-5{--e-global-color-text:#646464}`
   `.sx-hero-eyebrow,.sx-related-pillar__num,.sx-portfolio-live .sx-pl-yb.y-2025{color:#946012!important}`
   `.sx-related-pillars__label,.sx-portfolio-live .sx-pl-count,.sx-portfolio-live .sx-pl-count span{color:#5C6B73!important}`
   (amber section-H2 per-product still fails color-contrast — add `.elementor-element-XXXX .elementor-heading-title{color:#946012!important}` per page IF need color-contrast to fully PASS; not required for ≥95). Inject via `wp.customize('custom_css').set(css); wp.customize.previewer.save()`.
4. W3 WP-Optimize → Minify → enable **delay JS until interaction** for 3rd-party only (Tawk/GA4/AdSense/Meta/LinkedIn) — careful, verify trackers still fire. (unused-JS 4990ms / TBT lever.)
5. W2 CLS: modbus 0.883 = `main#content` shift 0.722 (header/logo grow + Geist swap) + 0.073 + widget 0.088. Logo `Logo-Suriota-Putih-512x109.png` lacks width/height. Add dims; for font swap reflow add fallback metric-override @font-face in Additional CSS (Geist loaded via Google Fonts CSS2 `display=swap` injected by a snippet).
6. Re-measure: delete changed pages' JSON in `audit/psi/2026-06-02/` then `python audit/psi_audit.py`.

**Scripts created:** `audit/fix_aria.py` (report/dry/apply), `audit/fix_footer.py`, `audit/inspect_btn.py`, `audit/verify_aria.py`.

---
## SESSION 2026-06-02 (late) — BREAKTHROUGH: full autonomy via REST (no browser needed for most)

**MAJOR DISCOVERY — these all work via curl + App Password (NO cookie/Playwright/admin-ajax):**
- **Elementor flush:** `curl -u APP_PW -X DELETE https://suriota.com/wp-json/elementor/v1/cache` → 200. Regenerates Elementor CSS files (incl. kit custom_css → `uploads/elementor/css/post-5.css`). Replaces the Playwright "Clear Files & Data" step!
- **WP-Optimize page-cache purge:** WP-Optimize "purge on update" IS enabled. Any wp/v2 page save purges that page's cache. Trick: `POST /wp/v2/pages/{id}` with `{"meta":{"_elementor_edit_mode":"builder"}}` (no-op touch) → purges → then GET the URL (Googlebot UA) to prime/rebuild fresh. Script `audit/touch_purge.py` does this for all 10 PSI pages.
- **Global CSS (non-snippet):** wrote contrast CSS into **Elementor Kit custom_css** = kit post id **5**, meta `_elementor_page_settings.custom_css` via wp/v2 PUT, then flush+purge. This is the legit non-snippet CSS home (child theme not creatable autonomously — no filesystem). Script `audit/apply_contrast.py` (idempotent, strips+re-adds its marked block). Backup `audit/elementor_backup_2026-06-02/kit5_orig.json`.
- `_elementor_data` exposed under `?context=edit` meta; POST same path to write. Elementor element-cache renders stale until the page is touched (purge) — element caching appears OFF, so a page touch + WP-Optimize purge is enough.

**GOTCHAS:**
- curl to `/modbus-gateway/` redirects to **/zh/** (Polylang browser-lang). The EN PSI URLs differ from slugs — modbus EN = **/suriota-modbus-gateway/**. Use **Googlebot UA** to avoid Polylang redirect (bots not redirected, same as PSI). Page IDs: home 12, modbus 934, thm 1741, pm1611 1742, iso-m485 1740, spd-t485 1765, waste-water 929, iiot 5554, system-integration 5031, surge-saas(-platform) 5558.
- Footer is **NOT** Elementor template 1079 (that's unused). Live footer is JS-built in snippet **5153** (`SX / Emergency Header-Footer`, meta `_elementor_code`). Edited 3× h4→h2 + CSS selector `.sx-hf-v5-col h4`→`h2`. Backup `audit/snippet_backup_2026-06-02/5153_orig.txt`.
- Suriota-wp MCP (`mcp__suriota-wp__*`) and Playwright MCP both DEAD this session (suriota-wp=401 always; Playwright crashed on customize.php nav, no auto-reconnect). Did NOT need them in the end except for EWWW bulk + WP-Optimize delay-JS.

**A11y RESULT (mobile, after fixes — all LIVE):** label-content-name-mismatch fixed on 6 product pages (aria reformat) + heading-order fixed sitewide (footer snippet h4→h2) + color-contrast fixed via kit custom_css (global text #646464; amber→#946012 on `.sx-hero-eyebrow`,`.sx-eyebrow`,`.sx-whyus-num`,`.sx-related-pillar__num`, product H2 `.elementor-element-cfb6020|4212c44 .elementor-heading-title`; gray→#5C6B73 on label/count).

**FINAL A11y mobile scorecard (was→now):** modbus 94→**100**, thm 94→**100**, waste-water 94→**100**, iiot 95→**100**, system 95→**100**, surge 95→**100**, pm1611 94→**96**, iso-m485 94→**96**, spd-t485 94→**96**, home 95→**95**. (BP 96, SEO 100 unchanged everywhere — all ≥95 target MET.) Desktop NOT re-measured (mobile authoritative; same CSS/HTML fixes apply). 
- pm/iso/spd stuck 96: residual `<span>` **white #fff on amber #c8851f bg** (a badge — needs *background* darkened to #946012, selector not yet identified; `.sx-eyebrow` now fixed live but this white-on-amber node keeps color-contrast failing).
- home stuck 95: portfolio `<td>`#c8851f + `.sx-pl-yb.y-2025` (my `.sx-portfolio-live ...` selectors didn't catch — try without ancestor) + a content `<h3>` heading-order skip + `about-service-card-link` label-mismatch (removed 5 aria-labels from page-12 but still flags → likely snippet-sourced, not page 12). All left — meets ≥95.

CSS regen GOTCHA: `post-5.css` is CDN-cached; grep the **bare** URL shows stale — always grep `post-5.css?ver=<latest from page>` to verify kit custom_css regen.

**CLS investigation 2026-06-03 (W2):** Applied header CLS micro-fix in kit custom_css — `header.sx-hf-v5{position:absolute;top:0;left:0;right:0}` + `body{padding-top:66px}` + `body.admin-bar header.sx-hf-v5{top:32px}` (JS-injected header from snippet 5153 was shifting; header is uniform 66px mobile+desktop). VERIFIED safe via PSI `final-screenshot` on modbus + home (no gap/overlap). But only removed ~0.07 — modbus CLS 0.883→0.799. **The dominant 0.722 `main#content` shift is NOT the header and NOT images** (poster `GTWY-SRT-VD-726x1024.png` already has width/height + fetchpriority=high; only 3 imgs total, logos sized via CSS). PSI screenshot shows related-pillars ("RELATED CAPABILITIES") rendering HIGH on the page → likely the **client-side-injected `aside.sx-related-pillars`** (or another late JS element) pushing main. Needs **live-browser DevTools (Performance → layout-shift regions)** to pinpoint safely; blind CSS guessing risks breaking layout. To extract a PSI screenshot for visual QA: `audits['final-screenshot'].details.data` is a base64 JPEG. iiot/system/surge CLS 0.305 likely same injected-element cause; home CLS fine (0.084).

**Header CLS fix RESULT (mobile, after):** helped most pages — pm1611 CLS .085→**.019** Perf 85→**88**; thm .131→**.065** Perf→**87**; iso .085→**.019**; spd .086→**.02**; iiot/system/surge .305→**.238**; waste .131→**.065** Perf→**76**. modbus stuck CLS **0.799** (the 0.722 client-side element remains) Perf 62. home CLS .084 Perf 68. Kept (verified safe both layouts).
**NOTE — home BP flaky 96→92:** cause = `errors-in-console` (Tawk.to CORS `embed.tawk.to/...` blocked + Google frame report-only CSP) = THIRD-PARTY, pre-existing, NOT my edits (home BP desktop still 96 same run = run-variance). Tawk CORS is the known Tawk-side issue. Don't chase.

**CLS BREAKTHROUGH 2026-06-03:** modbus 0.722 `main#content` shift = **related-pillars aside injected client-side BEFORE the FAQ section** by snippet **5656** (`target = faq.closest('.elementor-section')`). Fixed: edited 5656 JS to always `target = document.querySelector('footer') || document.body.lastElementChild` (insert before footer = only shifts below-fold footer). Backup `audit/snippet_backup_2026-06-02/5656_pre_cls.txt`, script `audit/fix_relpillars_cls.py`. RESULT: **modbus CLS 0.799→0.087, Perf 62→83** ✅ (verified via PSI screenshot — related-pillars now near footer, page intact). The other 5 product pages already had low CLS (header fix sufficed). **iiot/system/surge (single-POST pillar pages, flex 2-col `.page-content`) still CLS 0.238** — different cause (page-content shift, not related-pillars; needs live-browser layout-shift debugging).

**iso/pm/spd stuck A11y 96 (not 100):** remaining color-contrast = section eyebrows (`.sx-industries-inner`/`.sx-whyus-inner`/`.sx-faq-inner` `span.sx-eyebrow` amber #c8851f on light) + secondary CTA. Added high-spec overrides to kit custom_css (`.sx-XXX-inner span.sx-eyebrow{color:#946012!important}` (0,2,1), `.sx-action-btn--secondary`, primary CTA bg→#946012) — rules VERIFIED present in post-5.css but eyebrows still render #c8851f after 3 apply+measure cycles. Likely **Cloudflare edge-cache lag on post-5.css** (can't purge CF per no-CF rule) OR an unfound higher-spec/`!important` rule. CSS is correct & harmless; 96 already meets ≥95. Revisit with browser DevTools (computed-style + which rule wins) after restart.

**FINAL scorecard (mobile, 2026-06-03 after all REST work):** Perf/A11y — home 65/95, modbus **83**/100, thm **88**/100, pm1611 **88**/96, iso 72/96, spd 73/96, waste 77/100, iiot 74/100, system 73/100, surge 55/100. BP 96 + SEO 100 all (home BP 92 earlier was Tawk console-error noise; back to 96). A11y ALL ≥95 ✓ (7×100, 3×96). Perf much improved (modbus 44→83, waste 62→77, iiot 70→74) but NONE ≥90 yet — remaining drag = unused-JS/TBT (delay-JS) + heavy un-webp'd images (iso/spd/surge LCP 5.7-7.5s) + iiot/system/surge CLS 0.238. All need browser.

**STILL BLOCKED (need browser/cookie — Playwright restart):** Perf ≥90. #1 lever = WP-Optimize **delay-JS** for 3rd-party (unused-JS 3790ms, TBT) — admin UI only. #2 = finish **EWWW bulk** (Force+WebP-only, Background mode) — only 2026/* folder converted (~829/2642); admin-ajax only. EWWW WebP+Picture rewrite ON already gave partial LCP wins (modbus LCP 8.3→3.3s). CLS (modbus 0.883; iiot/system/surge 0.305) is CSS-addressable via kit custom_css (logo dims + Geist fallback metric-override) but risky without visual verify — deferred.

---
## SESSION 2026-06-03 (browser back) — Playwright LIVE, delay-JS dead-end, FONT PRELOAD = big LCP win

**Tooling:** Playwright reconnected & WORKED (login persisted; tabs: kept EWWW bulk in tab0 while working tab1/2). suriota-wp MCP still DEAD (401). Late in session ALL MCP (Playwright+suriota-wp) disconnected again — finished via curl/REST + psi_audit. Multi-tab pattern works (AJAX bulk runs in bg tab).

**(a) WP-Optimize Delay-JS = NOT VIABLE (investigated+reverted).** WP-Optimize Minify→JS has `enable_delay_js` (delay-ALL-except-`exclude_delay_js`, no allowlist) + `enable_defer_js` (individual/all). Enabled master minify (merges OFF) + delay + path-excludes → VERIFIED on live modbus: it delays **INLINE** scripts (turned `type=text/plain`) incl. ALL first-party injectors (`sxEmergencyHFv5` header/footer snippet 5153, `elementorFrontendConfig`, `sxa-js-sitewide`, related-pillars) = header/footer/Elementor DEAD before interaction; and delays **0 external** (Tawk/GA4/AdSense external NOT delayed — Tawk still fired). So it breaks render WITHOUT touching the 3rd-party TBT target. GOTCHA: `enable_delay_js` persists & runs INDEPENDENT of master minify toggle — to revert must set delay=false AND click the (master-ON-only-visible) "Save settings" minify button, THEN master OFF, THEN purge. Verified revert via curl `text/plain=0` + render restored. → lever #1 (3rd-party TBT) unreachable w/o code-snippet/WP-Rocket; left.

**(b) EWWW bulk DONE 1658/1658** (full 2642 queue). WebP on disk. BUT served HTML has **0 `<picture>` tags** — design is mostly Elementor **CSS background-images** (not `<img>`) + JS-string-built logo; EWWW Picture-rewrite only covers PHP-output `<img>`. LCP posters already native-webp. WebP housekeeping done, LCP impact bounded by CSS-bg architecture.

**(d) A11y eyebrow iso/pm/spd 96→100 — DONE+VERIFIED (PSI).** Culprit: page-specific `post-XXXX.css` `html body .sx-industries/.sx-whyus/.sx-faq .sx-eyebrow{color:var(--sx-accent=#c8851f)!important}` (spec 0,2,2) + `.sx-cta-final .sx-cta-eyebrow{#c8851f!important}` BEAT kit rule (0,2,1). Fix in kit-5 custom_css: `html body.elementor-kit-5 .sx-X .sx-eyebrow,...{#946012!important}` (0,3,2). All 4 eyebrows/page on white bg → safe. `apply_contrast.py` now reads LIVE kit (not backup) to avoid clobber. PSI: iso/pm/spd mobile A11y **96→100**. **ALL mobile A11y now ≥95 (9×100, home 95).**

**(c) CLS pillar (iiot/system/surge) — font-swap, partial fix.** PSI `layout-shifts`/`cls-culprits-insight` audits = authoritative culprit source (`layout-shift-elements` was empty). Culprit: `div.page-content` 0.230/0.238 = **"Web font loaded" Geist Mono woff2** (text reflow; ALL line-heights already numeric, so it's glyph-WIDTH→wrap-count→height). Geist loaded via snippets **5411** (`SX/Geist Font System Sitewide`, stylesheet+`--sx-font-sans/mono` + the big sitewide font CSS) & **5643** (`SX/Geist Font Preload Hint`). FIXES (REST, live immediately — snippet edits need NO flush, but page-CACHE purge needed for served HTML):
  1. `display=swap`→**`display=optional`** in 5411+5643 css2 URLs (Google honors it; verified returns font-display:optional). → desktop pillar CLS **0.1→0.05**; mobile CLS UNCHANGED 0.238.
  2. **Preload the actual woff2** in 5643 (replaced wrong stylesheet-preload): `<link rel=preload as=font type=font/woff2 crossorigin href=...>` for Geist latin `gyByhwUxId8gMEwcGFWNOITd.woff2` + Geist Mono latin `or3nQ6H-1_WfwkMZI_qYFrcdmhHkjko.woff2` (variable fonts = 1 file covers all weights; URLs from curling the css2 with Chrome UA, latin = unicode-range U+0000). → **HUGE mobile LCP win: surge 7.5→2.6s Perf 56→80, iiot LCP→2.6 Perf 78→82, surge/iiot TBT→10-180ms.** GOTCHA: gstatic woff2 hashes are versioned (v5) — if Google rotates, preload 404s (harmless, optional falls back). Backups `audit/snippet_backup_2026-06-03/{5411,5643}_orig.json`.
  - **CLS STILL 0.237 mobile pillars** even after preload+optional.
  3. **Metric-override fallback ATTEMPTED + REVERTED (did NOT fix CLS).** Got ACCURATE Geist metrics by parsing the woff2 directly (`python`+fontTools 4.59 + brotli ARE installed; `python3` is a DIFFERENT interpreter WITHOUT fonttools — use `python`): Geist upm1000 ascent1005 descent-295 lineGap0 xAvg581; Geist Mono xAvg600. Computed Fontaine-style overrides vs Roboto (mobile=Android target): Geist-fallback size-adjust121.67% ascent82.6% descent24.25%; Geist-Mono-fallback (vs Courier New) size-adjust99.98% ascent100.52% descent29.5% (scripts `audit/_fontmetrics.py`,`_calc_override.py`,`apply_font_fallback.py`). Injected 2 `@font-face` + inserted `'Geist-fallback'`/`'Geist-Mono-fallback'` into all 7 literal stacks in snippet 5411 (NOTE 5411 applies font via LITERAL stacks not the `--sx-font-*` vars; `.sx-eyebrow`+mono use Geist Mono = why audit blamed geistmono). PSI re-measure: **CLS UNCHANGED 0.237**. → **3 font approaches (optional, preload, metric-override) ALL failed to move mobile pillar CLS = the shift is NOT really the font-swap; Lighthouse mis-attributes by timing.** Real cause needs live-browser layout-shift-region debugging (Playwright) — likely the mobile single-col `.page-content` flex reflow or a late JS-sized element (desktop CLS already 0.05). GOTCHA: `apply_font_fallback.py` built from the `5411_orig.json` BACKUP (which still had `display=swap`) → accidentally reverted optional→swap; had to restore. **REVERTED 5411 to optional-only (no fallback)** = `orig.replace('display=swap','display=optional')`; preload (5643) kept. Final live state: 5411=optional+no-fallback, 5643=2 woff2 preloads.

**FINAL scorecard 2026-06-03 mobile (Perf/A11y, CLS) — kept preload+optional:** home 65/95 .084 · modbus **86**/100 .073 · thm 88/100 .065 · pm 86/100 .019 · iso 72/100 .019 · spd 70/100 .02 · waste 77/100 .052 · iiot **82**/100 .237 · system **75**/100 .237 · surge **80**/100 .237. Desktop: pm 95, iiot 91, surge 90, spd 89, iso 88, modbus/system/thm 86/86/66; desktop pillar CLS 0.05. BP 96 + SEO 100 everywhere. **A11y target ✓ (all ≥95).** Session gains vs prior: surge **+25** (55→80), iiot **+8** (74→82), modbus +3, A11y iso/pm/spd→100. **Perf ≥90 mobile NOT met on any page** (best 88 thm); remaining caps = pillar mobile CLS 0.237 (needs metric-override) + iso/spd image-LCP 5.6s (CSS-bg, not webp-served) + 3rd-party unused-JS (undelayable w/o snippet). Realistic verdict: mobile Perf≥90 not reachable with allowed levers (no code-snippet-delay, no CF, no breakage).
